{
  "name": "firebase",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "fb-login": "firebase login",
    "fb-init": "firebase init functions",
    "deploy": "npm install && firebase deploy --only functions --dd"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "firebase-tools": "^8.1.1"
  },
  "dependencies": {
    "firebase-admin": "^8.11.0",
    "firebase-functions": "^3.6.0",
    "firebase-tools": "8.1.1"
  }
}
